# Fast Codex workflow

Use narrow requests. Example:

> Work only on Stage 2. Improve the existing Hero while preserving section order, anchors, all other sections, and current functionality. First state the files you will touch. Then implement, run `npm run build`, report, and stop.

Avoid broad requests such as “redo everything” or combining design, SEO, CMS, and architecture in one task.
